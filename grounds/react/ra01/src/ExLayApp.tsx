import styled from '@emotion/styled';

const Container = styled.div`
  text-align: center;
`;

function ExLayApp() {
    return (
        <Container>
        </Container>
    );
}

export default ExLayApp;